# How CF maps to Zarr and relates to GeoZarr — CF Workshop 2026 talk

## Viewing slides

Open `index.html` in a browser (network required, reveal.js and fonts load from CDNs), or serve the directory:

```bash
python3 -m http.server 8000
# open http://localhost:8000/index.html
```
